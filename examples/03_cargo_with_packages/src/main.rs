/**
 * Cargo 官方包管理网址：https://crates.io/
 *
 * 可以使用命令行 `cargo search PACKAGE_NAME` 进行搜索
 *
 * 在 Cargo.toml 的 dependencies 中新增包，如：
 *
 * [dependencies]
 * rand = "0.8.4"
 */

fn main() {
    println!("Hello, world!");
}
